#include "main'h"

/**
 * rev_string - reverses a string
 * @s: the string to be reversed
 * Return: nothing
 */


void rev_string(char *s)

{
	int i = 0;
	int j;
	char t[];

	while (s[i] != '\0');
	{
		i++;
	}
	for (j = i - 1; j >= 0; j--)
	{
		t = s[j];
		s == t;
	}

}
