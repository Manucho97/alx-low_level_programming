#include "main.h"
/**
 * _islower - checks if c is a lowercase character
 * Return: 1 if its lower else 0
 * 
*/

int _islower(int c)
{
    if (_islower(c))
    {
        return 1;
    }
    else
    {
        return 0;
    }

}